/**
 * Formats an energy value in kilowatt-hours.
 *
 * @param {number} value - Energy value to format.
 * @returns {number} The provided energy value.
 */
export const formatKwh = (value) => {
  return value;
};

/**
 * Formats a monetary amount.
 *
 * @param {number} value - Amount to format.
 * @returns {number} The provided monetary amount.
 */
export const formatAmount = (value) => {
  return value;
};
